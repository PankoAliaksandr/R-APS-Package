\name{get_cov_ma}
\alias{get_cov_ma}
\title{get_cov_ma}
\usage{get_cov_ma(cov_id = NA,
                       portfolio,
                       cov_ret,
                       calc_method,
                       sec_names)}
\description{get risk contributions of active weights}
\arguments{
  \item{cov_id}{covariance matrix ID id you know it}
  \item{portfolio}{the corresponding portfolio if covariance matrix should be found}
  \item{cov_ret}{type of returns}
  \item{calc_method}{type of calculation method}
  \item{sec_names}{a vector with constituents to check if the order is the same}
}
%\examples{isDate(mydate, format = "\%Y-\%m-\%d")}
